---
title: Toast
description: A non-intrusive notification component that provides brief messages to the user.
---

import ComponentPreview from '../../../components/ComponentPreview.astro';
import ComponentTypes from '../../../components/ComponentTypes.astro';
import FrameworkContent from '../../../components/FrameworkContent.astro';

> A non-intrusive notification component that provides brief messages to the user.

<ComponentPreview component="toast" example="Basic" />

## Anatomy

<FrameworkContent fw="vue">
```vue
<script setup lang="ts">
import { Toast, Toaster, createToaster } from '@destyler-ui/vue'

const toaster = createToaster({ placement: 'bottom-end' })
</script>

<template>
  <button type="button" @click="toaster.create({ title: 'Upload complete', description: 'Your file is ready.' })">
    Create toast
  </button>
  <Toaster :toaster="toaster" v-slot="toast">
    <Toast.Root>
      <Toast.Title>{{ toast.title }}</Toast.Title>
      <Toast.Description>{{ toast.description }}</Toast.Description>
      <Toast.ActionTrigger>Undo</Toast.ActionTrigger>
      <Toast.CloseTrigger>Close</Toast.CloseTrigger>
    </Toast.Root>
  </Toaster>
</template>
```
</FrameworkContent>

<FrameworkContent fw="react">
```tsx
import { useMemo } from 'react'
import { Toast, Toaster, createToaster } from '@destyler-ui/react'

export default function Basic() {
  const toaster = useMemo(() => createToaster({ placement: 'bottom-end' }), [])

  return (
    <>
      <button type="button" onClick={() => toaster.create({ title: 'Upload complete', description: 'Your file is ready.' })}>
        Create toast
      </button>
      <Toaster toaster={toaster}>
        {toast => (
          <Toast.Root key={toast.id}>
            <Toast.Title>{toast.title}</Toast.Title>
            <Toast.Description>{toast.description}</Toast.Description>
            <Toast.ActionTrigger>Undo</Toast.ActionTrigger>
            <Toast.CloseTrigger>Close</Toast.CloseTrigger>
          </Toast.Root>
        )}
      </Toaster>
    </>
  )
}
```
</FrameworkContent>

<FrameworkContent fw="solid">
```tsx
import { Toast, Toaster, createToaster } from '@destyler-ui/solid'

export default function Basic() {
  const toaster = createToaster({ placement: 'bottom-end' })

  return (
    <>
      <button type="button" onClick={() => toaster.create({ title: 'Upload complete', description: 'Your file is ready.' })}>
        Create toast
      </button>
      <Toaster toaster={toaster}>
        {toast => (
          <Toast.Root>
            <Toast.Title>{toast().title}</Toast.Title>
            <Toast.Description>{toast().description}</Toast.Description>
            <Toast.ActionTrigger>Undo</Toast.ActionTrigger>
            <Toast.CloseTrigger>Close</Toast.CloseTrigger>
          </Toast.Root>
        )}
      </Toaster>
    </>
  )
}
```
</FrameworkContent>

<FrameworkContent fw="svelte">
```svelte
<script lang="ts">
  import { Toast, Toaster, createToaster } from '@destyler-ui/svelte'

  const toaster = createToaster({
    placement: 'bottom-end',
    overlap: true,
    gap: 24,
  })
</script>

{#snippet title()}Upload complete{/snippet}
{#snippet description()}Your file is ready.{/snippet}

<button
  type="button"
  onclick={() => toaster.create({ title, description, type: 'info' })}
>
  Create toast
</button>

<Toaster {toaster}>
  {#snippet children(toast)}
    <Toast.Root>
      <Toast.Title>{@render toast().title?.()}</Toast.Title>
      <Toast.Description>{@render toast().description?.()}</Toast.Description>
      <Toast.ActionTrigger>Undo</Toast.ActionTrigger>
      <Toast.CloseTrigger>Close</Toast.CloseTrigger>
    </Toast.Root>
  {/snippet}
</Toaster>
```
</FrameworkContent>

## Examples

### Basic

<ComponentPreview component="toast" example="Basic" />

### Toast Types

Display different toast types (success, error, warning, info).

<ComponentPreview component="toast" example="ToastTypes" />

### Toast Action

Add action buttons to toast notifications.

<ComponentPreview component="toast" example="ToastAction" />

### Toast Placement

Configure the toast placement position.

<ComponentPreview component="toast" example="ToastPlacement" />

### Toast Promise

Display a toast based on a promise result.

<ComponentPreview component="toast" example="ToastPromise" />

### Toast Update

Update an existing toast notification.

<ComponentPreview component="toast" example="ToastUpdate" />

### Action

Handle action trigger events.

<ComponentPreview component="toast" example="Action" />

### Update

Update toast content dynamically.

<ComponentPreview component="toast" example="Update" />

## API Reference

<ComponentTypes id="toast" />
