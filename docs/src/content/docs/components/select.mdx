---
title: Select
description: A component that allows users to choose from a list of options in a dropdown.
---

import ComponentPreview from '../../../components/ComponentPreview.astro';
import ComponentTypes from '../../../components/ComponentTypes.astro';
import FrameworkContent from '../../../components/FrameworkContent.astro';

> A component that allows users to choose from a list of options in a dropdown.

<ComponentPreview component="select" example="Basic" />

## Anatomy

<FrameworkContent fw="vue">
```vue
<script setup lang="ts">
import { Select } from '@destyler-ui/vue'
</script>

<template>
  <Select.Root>
    <Select.Label />
    <Select.Control>
      <Select.Trigger>
        <Select.ValueText />
        <Select.Indicator />
      </Select.Trigger>
      <Select.ClearTrigger />
    </Select.Control>
    <Select.Positioner>
      <Select.Content>
        <Select.ItemGroup>
          <Select.ItemGroupLabel />
          <Select.Item>
            <Select.ItemText />
            <Select.ItemIndicator />
          </Select.Item>
        </Select.ItemGroup>
      </Select.Content>
    </Select.Positioner>
    <Select.HiddenSelect />
  </Select.Root>
</template>
```
</FrameworkContent>

<FrameworkContent fw="react">
```tsx
import { Select } from '@destyler-ui/react'

export default function Basic() {
  return (
    <Select.Root>
      <Select.Label />
      <Select.Control>
        <Select.Trigger>
          <Select.ValueText />
          <Select.Indicator />
        </Select.Trigger>
        <Select.ClearTrigger />
      </Select.Control>
      <Select.Positioner>
        <Select.Content>
          <Select.ItemGroup>
            <Select.ItemGroupLabel />
            <Select.Item>
              <Select.ItemText />
              <Select.ItemIndicator />
            </Select.Item>
          </Select.ItemGroup>
        </Select.Content>
      </Select.Positioner>
      <Select.HiddenSelect />
    </Select.Root>
  )
}
```
</FrameworkContent>

<FrameworkContent fw="svelte">
```svelte
<script lang="ts">
  import { Select, createListCollection } from '@destyler-ui/svelte'

  const collection = createListCollection({
    items: [
      { label: 'React', value: 'react' },
      { label: 'Vue', value: 'vue' },
      { label: 'Svelte', value: 'svelte' },
    ],
  })
</script>

<Select.Root {collection}>
  <Select.Label>Framework</Select.Label>
  <Select.Control>
    <Select.Trigger>
      <Select.ValueText placeholder="Select a framework" />
      <Select.Indicator>⌄</Select.Indicator>
    </Select.Trigger>
    <Select.ClearTrigger>Clear</Select.ClearTrigger>
  </Select.Control>
  <Select.Positioner>
    <Select.Content>
      <Select.ItemGroup>
        <Select.ItemGroupLabel>Frameworks</Select.ItemGroupLabel>
        {#each collection.items as item (item.value)}
          <Select.Item {item}>
            <Select.ItemText>{item.label}</Select.ItemText>
            <Select.ItemIndicator>✓</Select.ItemIndicator>
          </Select.Item>
        {/each}
      </Select.ItemGroup>
    </Select.Content>
  </Select.Positioner>
  <Select.HiddenSelect />
</Select.Root>
```
</FrameworkContent>

## Examples

### Basic

<ComponentPreview component="select" example="Basic" />

### Advanced

Advanced select usage with custom item rendering.

<ComponentPreview component="select" example="Advanced" />

### Controlled

Control the selected value programmatically.

<ComponentPreview component="select" example="Controlled" />

### Multiple

Enable multiple item selection.

<ComponentPreview component="select" example="Multiple" />

### Reactive Collection

Use a reactive collection for dynamic options.

<ComponentPreview component="select" example="ReactiveCollection" />

### With Field

Combine with a form field for validation and labeling.

<ComponentPreview component="select" example="WithField" />

### Root Provider

<ComponentPreview component="select" example="RootProvider" />

## API Reference

<ComponentTypes id="select" />
