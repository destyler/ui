import { {{capitalize (camelize react)}} } from '../index'

export function Basic() {
  return (
    <{{capitalize (camelize react)}}.Root>
      {/* Your component example here */}
    </{{capitalize (camelize react)}}.Root>
  )
}
