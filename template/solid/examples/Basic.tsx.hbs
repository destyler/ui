import { {{capitalize (camelize solid)}} } from '../index'

export const Basic = () => (
  <{{capitalize (camelize solid)}}.Root>
    {/* Your component example here */}
  </{{capitalize (camelize solid)}}.Root>
)
